git --version
git config --global user.name ""
git config --global user.email 
git config --list
git init ---> to intialise as git repository
git status  ---> to check the status of the directory
git add . --->this will include all the files to be commited in the git can also give perticular file name
git commit --->commit the changes to the git repository
git commit -m "asdf" ---> -m is for the message and it is written in the brackets
git log --oneline  ---> it is used to show the logs of all commits
git checkout commitcode filename ---> this is used to rollback to the previous place
git reset HEAD filename --->
git restore filename ---->to retrieve back

git remote add origin url ----> to add the repositoryto a remote repository
git push -u oririn master// git push --force origin master---> the push command is used to upload the lcoal repository code to the remote repository
git clone repository url ---. is used to clone the repositoryfrom remote to local